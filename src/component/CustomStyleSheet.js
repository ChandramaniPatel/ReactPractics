/* eslint-disable no-unused-vars */
/* eslint-disable no-undef */
import { View, StyleSheet } from 'react-native';
import React, { Component, } from 'react';

const CustomStylesSheet = StyleSheet.create({
bigValue: {
    fontWeight: 'bold',
    fontSize: 30,
 },
 tileColor: {
     color: '#AAFF55'
 },
});

export default CustomStylesSheet;
